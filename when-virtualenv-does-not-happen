sudo apt-get purge --auto-remove python-pip

sudo apt-get install python-pip (python3-pip)

(i tried that one but add sudo) pip install virtualenv (try with pip3)

sudo -H pip install virtualenv


then virtualenv (name_of_virtual_environment)

source name/bin/activate


#
sudo apt-get remove python #removes python2


sudo apt-get install python3

sudo apt-get install python3-pip

sudo pip install virtualenv

virtualenv name

source name/bin/activate

python --version # be sure it is python3

(inside the environment I can use pip not pip3)
